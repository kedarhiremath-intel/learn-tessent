`reparameter LV_CONTROLLER_FLOPS = 0
`reparameter LV_COLLAR_FLOPS = 0
`update LV_CONTROLLER_FLOPS = LV_CONTROLLER_FLOPS + 974
`parameter LV_M1_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M1
`parameter LV_M2_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M2
`parameter LV_M3_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M3
`parameter LV_M4_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M4
`parameter LV_M5_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M5
`parameter LV_M6_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M6
`parameter LV_M7_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M7
`parameter LV_M8_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M8
`parameter LV_M9_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M9
`parameter LV_M10_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M10
`parameter LV_M11_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M11
`parameter LV_M12_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M12
`parameter LV_M13_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M13
`parameter LV_M14_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M14
`parameter LV_M15_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M15
`parameter LV_M16_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M16
`parameter LV_M17_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M17
`parameter LV_M18_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M18
`parameter LV_M19_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M19
`parameter LV_M20_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M20
`parameter LV_M21_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M21
`parameter LV_M22_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M22
`parameter LV_M23_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M23
`parameter LV_M24_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M24
`parameter LV_M25_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M25
`parameter LV_M26_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M26
`parameter LV_M27_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M27
`parameter LV_M28_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M28
`parameter LV_M29_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M29
`parameter LV_M30_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M30
`parameter LV_M31_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M31
`parameter LV_M32_COLLAR_FLOPS = 151
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 151 // 1 * M32
`parameter LV_M33_COLLAR_FLOPS = 270
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 270 // 1 * M33
`parameter LV_M34_COLLAR_FLOPS = 270
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 270 // 1 * M34
`parameter LV_M35_COLLAR_FLOPS = 270
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 270 // 1 * M35
`parameter LV_M36_COLLAR_FLOPS = 270
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 270 // 1 * M36
`parameter LV_M37_COLLAR_FLOPS = 171
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 171 // 1 * M37
`parameter LV_M38_COLLAR_FLOPS = 171
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 171 // 1 * M38
`parameter LV_M39_COLLAR_FLOPS = 171
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 171 // 1 * M39
`parameter LV_M40_COLLAR_FLOPS = 171
`update LV_COLLAR_FLOPS = LV_COLLAR_FLOPS + 171 // 1 * M40
